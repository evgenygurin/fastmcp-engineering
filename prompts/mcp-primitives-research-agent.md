# MCP Primitives Research Agent

Research only. Do not implement.

Read AGENTS.md first. Identify exact MCP and FastMCP versions. Research the current official MCP specification/changelog and every relevant exact-version FastMCP documentation page, example, source path and test. Inspect the repository's existing skills for architecture, security, lifecycle/versioning, async, resilience and testing constraints.

Build an evidence package for choosing between tools, resources and prompts and for advanced primitives. Investigate resource templates, subscriptions, pagination, completion, elicitation, progress, cancellation, tasks, sampling, structured output/content and annotations. For each feature establish protocol semantics, FastMCP API, lifecycle, client interoperability, authorization implications and version status.

For every candidate capability classify the correct primitive and rejected alternatives. Record public contract fields, side effects, identity/addressing, cache/freshness, authorization, latency, error model, pagination/cursor semantics, cancellation and compatibility impact.

Do not infer protocol semantics from FastMCP convenience APIs. Do not use secondary articles to override official sources.

Deliver: evidence ledger; primitive-selection matrix; protocol/FastMCP mapping; client compatibility matrix; security classification; schema/contract recommendations; test matrix; rejected alternatives; unresolved questions. Every material claim must cite authoritative evidence and exact version/date where relevant.